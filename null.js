// Assume we have an array of keys
const keys = ['a', 'b', 'c', 'd'];

// Using for...of to iterate over each key in the array
for (const key of keys) {
  console.log(key); // This will log 'a', then 'b', then 'c', then 'd'
}
